// Fill out your copyright notice in the Description page of Project Settings.


#include "TopDownCameraActor.h"
#include "Camera/CameraComponent.h"


ATopDownCameraActor::ATopDownCameraActor()
{
    //// Set this actor to call Tick() every frame
    //PrimaryActorTick.bCanEverTick = false;

    //// Create the camera component
    //Camera = CreateDefaultSubobject<UCameraComponent>(TEXT("CameraComponent"));
    //RootComponent = Camera;

    //// Set camera properties (adjust these values as needed)
    //Camera->SetRelativeLocation(FVector(0, 0, 500)); // Adjust the camera's location
    //Camera->SetRelativeRotation(FRotator(-90, 0, 0)); // Adjust the camera's rotation for top-down view
    //Camera->SetFieldOfView(90.0f); // Adjust FOV as needed
}
